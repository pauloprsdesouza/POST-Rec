# Auto-generated from deploy/apps/registry.json — do not edit manually.
# Re-run: python scripts/generate_traefik_apps.py --write

http:
  routers:
    researchly-http:
      rule: (Host(`paulorobertosouza.com.br`) || Host(`www.paulorobertosouza.com.br`)) && PathPrefix(`/researchly`)
      entryPoints: [http]
      middlewares: [redirect-to-https]
      service: app-researchly
      priority: 100

    researchly-https:
      rule: (Host(`paulorobertosouza.com.br`) || Host(`www.paulorobertosouza.com.br`)) && PathPrefix(`/researchly`)
      entryPoints: [https]
      middlewares: [security-headers]
      service: app-researchly
      tls:
        certResolver: letsencrypt
      priority: 100

    evolution-http:
      rule: (Host(`paulorobertosouza.com.br`) || Host(`www.paulorobertosouza.com.br`)) && PathPrefix(`/evolution`)
      entryPoints: [http]
      middlewares: [redirect-to-https, strip-evolution]
      service: app-evolution
      priority: 100

    evolution-https:
      rule: (Host(`paulorobertosouza.com.br`) || Host(`www.paulorobertosouza.com.br`)) && PathPrefix(`/evolution`)
      entryPoints: [https]
      middlewares: [security-headers, strip-evolution]
      service: app-evolution
      tls:
        certResolver: letsencrypt
      priority: 100

    evolution-root0-http:
      rule: (Host(`paulorobertosouza.com.br`) || Host(`www.paulorobertosouza.com.br`)) && PathPrefix(`/manager`)
      entryPoints: [http]
      middlewares: [redirect-to-https]
      service: app-evolution
      priority: 105

    evolution-root0-https:
      rule: (Host(`paulorobertosouza.com.br`) || Host(`www.paulorobertosouza.com.br`)) && PathPrefix(`/manager`)
      entryPoints: [https]
      middlewares: [security-headers]
      service: app-evolution
      priority: 105
      tls:
        certResolver: letsencrypt

    evolution-root1-http:
      rule: (Host(`paulorobertosouza.com.br`) || Host(`www.paulorobertosouza.com.br`)) && PathPrefix(`/assets`)
      entryPoints: [http]
      middlewares: [redirect-to-https]
      service: app-evolution
      priority: 105

    evolution-root1-https:
      rule: (Host(`paulorobertosouza.com.br`) || Host(`www.paulorobertosouza.com.br`)) && PathPrefix(`/assets`)
      entryPoints: [https]
      middlewares: [security-headers]
      service: app-evolution
      priority: 105
      tls:
        certResolver: letsencrypt

    portainer-http:
      rule: (Host(`paulorobertosouza.com.br`) || Host(`www.paulorobertosouza.com.br`)) && PathPrefix(`/portainer`)
      entryPoints: [http]
      middlewares: [redirect-to-https]
      service: app-portainer
      priority: 100

    portainer-https:
      rule: (Host(`paulorobertosouza.com.br`) || Host(`www.paulorobertosouza.com.br`)) && PathPrefix(`/portainer`)
      entryPoints: [https]
      middlewares: [security-headers]
      service: app-portainer
      tls:
        certResolver: letsencrypt
      priority: 100

    portainer-root0-http:
      rule: (Host(`paulorobertosouza.com.br`) || Host(`www.paulorobertosouza.com.br`)) && PathPrefix(`/locales`)
      entryPoints: [http]
      middlewares: [redirect-to-https]
      service: app-portainer
      priority: 105

    portainer-root0-https:
      rule: (Host(`paulorobertosouza.com.br`) || Host(`www.paulorobertosouza.com.br`)) && PathPrefix(`/locales`)
      entryPoints: [https]
      middlewares: [security-headers]
      service: app-portainer
      priority: 105
      tls:
        certResolver: letsencrypt

    portainer-root1-http:
      rule: (Host(`paulorobertosouza.com.br`) || Host(`www.paulorobertosouza.com.br`)) && PathPrefix(`/api`)
      entryPoints: [http]
      middlewares: [redirect-to-https]
      service: app-portainer
      priority: 105

    portainer-root1-https:
      rule: (Host(`paulorobertosouza.com.br`) || Host(`www.paulorobertosouza.com.br`)) && PathPrefix(`/api`)
      entryPoints: [https]
      middlewares: [security-headers]
      service: app-portainer
      priority: 105
      tls:
        certResolver: letsencrypt

    grafana-http:
      rule: (Host(`paulorobertosouza.com.br`) || Host(`www.paulorobertosouza.com.br`)) && PathPrefix(`/grafana`)
      entryPoints: [http]
      middlewares: [redirect-to-https]
      service: app-grafana
      priority: 100

    grafana-https:
      rule: (Host(`paulorobertosouza.com.br`) || Host(`www.paulorobertosouza.com.br`)) && PathPrefix(`/grafana`)
      entryPoints: [https]
      middlewares: [security-headers]
      service: app-grafana
      tls:
        certResolver: letsencrypt
      priority: 100

    www-redirect-http:
      rule: Host(`www.paulorobertosouza.com.br`)
      entryPoints: [http]
      middlewares: [www-to-apex]
      service: noop
      priority: 90

    www-redirect-https:
      rule: Host(`www.paulorobertosouza.com.br`)
      entryPoints: [https]
      middlewares: [www-to-apex]
      service: noop
      priority: 90
      tls:
        certResolver: letsencrypt

    landing-http:
      rule: Host(`paulorobertosouza.com.br`) && Path(`/`)
      entryPoints: [http]
      middlewares: [redirect-to-https]
      service: landing
      priority: 80

    landing-https:
      rule: Host(`paulorobertosouza.com.br`) && Path(`/`)
      entryPoints: [https]
      middlewares: [security-headers]
      service: landing
      priority: 80
      tls:
        certResolver: letsencrypt

    unknown-app-http:
      rule: (Host(`paulorobertosouza.com.br`) || Host(`www.paulorobertosouza.com.br`)) && PathPrefix(`/`) && !Path(`/`) && !PathPrefix(`/researchly`) && !PathPrefix(`/evolution`) && !PathPrefix(`/portainer`) && !PathPrefix(`/grafana`) && !PathPrefix(`/manager`) && !PathPrefix(`/assets`) && !PathPrefix(`/locales`) && !PathPrefix(`/api`)
      entryPoints: [http]
      middlewares: [redirect-to-https]
      service: unknown-app
      priority: 50

    unknown-app-https:
      rule: (Host(`paulorobertosouza.com.br`) || Host(`www.paulorobertosouza.com.br`)) && PathPrefix(`/`) && !Path(`/`) && !PathPrefix(`/researchly`) && !PathPrefix(`/evolution`) && !PathPrefix(`/portainer`) && !PathPrefix(`/grafana`) && !PathPrefix(`/manager`) && !PathPrefix(`/assets`) && !PathPrefix(`/locales`) && !PathPrefix(`/api`)
      entryPoints: [https]
      middlewares: [security-headers]
      service: unknown-app
      priority: 50
      tls:
        certResolver: letsencrypt

  middlewares:
    strip-evolution:
      stripPrefix:
        prefixes:
          - /evolution

    redirect-to-https:
      redirectScheme:
        scheme: https
        permanent: true

    security-headers:
      headers:
        browserXssFilter: true
        contentTypeNosniff: true
        frameDeny: true
        referrerPolicy: strict-origin-when-cross-origin
        permissionsPolicy: camera=(), microphone=(), geolocation=()
        stsSeconds: 31536000
        stsIncludeSubdomains: true
        stsPreload: true

    www-to-apex:
      redirectRegex:
        regex: '^https?://www\.paulorobertosouza\.com\.br/(.*)'
        replacement: "https://paulorobertosouza.com.br/${1}"
        permanent: true

  services:
    app-researchly:
      loadBalancer:
        servers:
          - url: http://postrec-web:80

    app-evolution:
      loadBalancer:
        servers:
          - url: http://postrec-evolution-api:8080

    app-portainer:
      loadBalancer:
        servers:
          - url: http://postrec-portainer-proxy:80

    app-grafana:
      loadBalancer:
        servers:
          - url: http://postrec-grafana-proxy:80

    landing:
      loadBalancer:
        servers:
          - url: http://postrec-landing:80

    unknown-app:
      loadBalancer:
        servers:
          - url: http://postrec-unknown-app:80

    noop:
      loadBalancer:
        servers:
          - url: http://127.0.0.1:9
