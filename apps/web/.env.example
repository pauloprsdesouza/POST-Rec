# Leave empty to use the Vite dev proxy (/api -> http://127.0.0.1:8000).
# On Windows, if login shows 500/502, set this to call the API directly:
# VITE_API_BASE_URL=http://127.0.0.1:8000
VITE_API_BASE_URL=
